// FavoriteSpots.js
import React from 'react';
import { Box, Typography } from '@mui/material';

const FavoriteSpots = () => {
  return (
    <Box>
      <Typography variant="h6" gutterBottom>
        Favorite Spots
      </Typography>
      <Typography>
        Your favorite parking spots will be displayed here.
      </Typography>
    </Box>
  );
};

export default FavoriteSpots;

